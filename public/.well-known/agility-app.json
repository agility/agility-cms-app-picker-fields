{
	"name": "Power Pickers",
	"version": "0.1.0",
	"sdkVersion": "2.0.0",
	"description": "Pickers for the two things that are always a plain text box: icons and colors. Six custom fields — four icon libraries, plus a hex color picker and a named color picker.",
	"documentationLink": "https://github.com/agility/agility-cms-app-pickers",
	"configValues": [
		{
			"name": "brandSwatches",
			"label": "Brand swatches",
			"type": "string",
			"description": "Quick-pick swatches shown in both color fields. Comma or newline separated, as '#0F62FE' or 'Brand Blue #0F62FE'. Leave blank for none."
		},
		{
			"name": "hexAllowAlpha",
			"label": "Allow transparency in the Hex Color field",
			"type": "string",
			"description": "Set to 'true' to add an opacity slider. Values stay #RRGGBB while fully opaque and become #RRGGBBAA once they are not. Defaults to off, because 8-digit hex is not safe everywhere."
		},
		{
			"name": "namedColorSet",
			"label": "Named color set",
			"type": "string",
			"description": "Replaces the 148 CSS named colors in the Named Color field with your own tokens, as 'brand-blue #0F62FE, brand-ink #101828'. Leave blank to use the CSS names."
		}
	],
	"capabilities": {
		"fields": [
			{
				"name": "icon-lucide",
				"label": "Icon — Lucide",
				"description": "Pick a Lucide icon. Stores the icon name, e.g. 'arrow-right'."
			},
			{
				"name": "icon-heroicons",
				"label": "Icon — Heroicons",
				"description": "Pick a Heroicon in any of its four styles. Stores 'style/name', e.g. 'outline/academic-cap'."
			},
			{
				"name": "icon-fontawesome",
				"label": "Icon — Font Awesome",
				"description": "Pick a Font Awesome Free icon. Stores 'style/name', e.g. 'solid/star' or 'brands/github'."
			},
			{
				"name": "icon-simple-icons",
				"label": "Icon — Simple Icons (brands)",
				"description": "Pick a brand logo from Simple Icons. Stores the slug, e.g. 'github'."
			},
			{
				"name": "color-hex",
				"label": "Color — Hex",
				"description": "Pick a color visually. Stores a hex value, e.g. '#0F62FE'."
			},
			{
				"name": "color-named",
				"label": "Color — Named",
				"description": "Pick from the CSS named colors, or from your own named tokens. Stores the name, e.g. 'rebeccapurple'."
			}
		]
	}
}
